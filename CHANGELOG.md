# Changelog
## [1.2.18] - 2026-09-05

### 新增

- **侧边栏补齐五/六级标题按钮**（H5/H6，工具箱「文本格式」分区，命令面板同步可用）。
- **侧边栏补齐有序/无序列表按钮**（命令面板中早就有，现在侧栏也能一键插入）。
- **用户模板支持直接修改内容**：「管理我的模板…」新增「修改内容」动作——把模板正文装入临时文档编辑，改完「保存当前文档为模板」同名覆盖即可。

### 修复

- **预览中很窄的（行间）公式不再出现滚动条**：滚动容器从 fit-content 的内层块上移到 100% 宽的外层包装（段内 `$$` 恢复时现自带包装），窄公式结构性不可能出现滚动条，真正超出窗宽的公式照常可滚动；经真实 Chromium 在两种窗宽/两种 DPR 下实测：所有窄公式均无滚动条、所有公式仍逐像素居中。
- **导出的 HTML 不再带「VSCode 缺少音频解码」提示**：该提示只在 VSCode 预览内成立，导出页在真实浏览器中 B 站视频可正常发声，提示按环境分流（预览保留、导出省略）。

## [1.2.17] - 2026-09-05

### 修复

- **段内 `$$…$$` 现在渲染为独立居中的行间公式**：任何双美元号（无论是否独占一行）都按 KaTeX auto-render / 洛谷实际渲染语义占独立居中行，修复连续三次报告的「行间公式不居中」（此前按 remark-math 把段内 `$$` 判为行内公式，观感就是不居中）。预览与导出一致；真实 Chromium 布局实测九种 `$$` 写法全部居中（左右间距逐像素相等）。
- **「切换滚动同步」不再可能因设置写入被拒而整体失败**：写入 `luogu-editor.scrollSync` 失败的极端环境（远程受限工作区、扩展宿主缓存未刷新等）下退化为仅本次会话生效，内存态与广播立即落地，并明确提示；此前在该环境报「没有注册配置」且开关完全不生效。
- **预览勾选任务项回写失败时不再静默**：源码行已变化/未找到绑定编辑器时，提示原因并把预览中已翻转的复选框回滚到真实状态，避免「预览看着勾上了、源码没变」的假象。

## [1.2.16] - 2026-08-30

### 新增

- **滚动同步总开关**：「工具」页新增「切换滚动同步」一键切换（也写入配置 `luogu-editor.scrollSync`，默认开）。双向均带门禁：主侧监听处 + webview 消息侧，关闭后两边都不跟随（预览不再被编辑区滚动拽走，编辑区也不回拽）。
- **自建模板库**：「模板」页新增「我的模板」分区。点「保存当前文档为模板」把当前 Markdown 入库（`globalState` 本机持久，工作区无关）；列表点项即插，支持重名询问覆盖、`管理我的模板…` 重命名/删除。

### 测试

- `test-user-templates.js` 纯逻辑测试 14 项（保存/覆盖门禁/非法名/ICU zh 排序/重命名撞名/删除幂等）全绿。

## [1.2.15] - 2026-08-30

### 修复

- **行间公式不居中（预览 + 导出都修）**：`.luogu-math-display` 仅靠 `text-align:center` 不够稳，受 KaTeX 0.18 `width:min-content` 内层与多重包层影响可回退左对齐。改为 `width: fit-content + margin: x auto` 块级居中，预览（`styles.css`）与导出（`EXPORT_CSS` 附加 `!important` 鞘装）两地同时生效。
- **导出 HTML 自包含**：KaTeX woff2 字体 20 个 face 全部以 `data:` URI 内嵌（约 +400KB），单独发出的 HTML 无需携带 fonts/ 目录。Volderka 版 HTML 的外部 fonts/ 方案弃用。
- **暗色页上 Ctrl+P 打印为通用亮色**：从 `styles.css :root` 抽出亮色变量集封装为 `@media print { [data-theme="dark"] ... }` 覆盖，即使导出页选择了暗色也能白盒打印（之前会糊黑）。

### 测试

- `test-export.js` +4：data URI 字体 ≥18 face / 打印亮印覆盖 / fit-content 居中鞘装。导出回归 14/14；其余 63 差分 + 49 对抗 + 42+58 回归全绿。

## [1.2.14] - 2026-08-30

### 修复

- **未知 directive 容器吞文档**：`:::cute-table{three}` 及其它未知名（如用户把叶子语法用成 3 冒号）此前被一股脑渲染成 info 折叠框，并吞掉容器内所有内容/未闭合时吞掉文档尾部。现严格按 remark-directive 默认语义**字面渲染开闭行、内容正常走解析**——与洛谷行为一致。`cute-table` 的 `:::` 严格三冒号手误同时兼容为叶子指示行。
- **导出 HTML 暗色配色全错**：`preview.css` 提供 `--vscode-*` 变量的浅色 fallback，在独立导出页（无 VSCode 变量注入）中以「后写同 specificity」压过 `styles.css` 的 `[data-theme="dark"]` 块。现把 `preview.css`（webview 专用基底）剔出导出链，改以自包含 `EXPORT_CSS` 基底，亮/暗主题均恢复正常。
- **导出 PDF 代码块颜色错误**：PDF 导出页（打印版）曾继承系统/localStorage 暗色主题整页打印，`@media print` 白底体色不匹配 → 打印前强制回亮色，代码块颜色正常。
- **导出 HTML 行间公式不居中**：`EXPORT_CSS` 加 `text-align:center` 鞘装于 `.luogu-math-block-wrap`/`.luogu-math-display`，杜绝包层/浏览器默认对齐差异。
- 导出页面自增 `<!-- generated by wudream.luogu-markdown-editor vX.Y.Z -->` 版本戳，便于未来报障定位生成版本。

### 测试

- 新增 `test-export.js`：导出主题自洽性/数学居中/打印亮印/版本戳 10 项结构性回归全绿。

## [1.2.13] - 2026-08-30

### 新增

- **cute-table 手册语法补齐**：`::cute-table{three}` 三线表（booktabs 风格：顶/底 2px 粗线 + 表头分隔细线，亮暗主题自适应）；`::cute-table{tuack=N[,M…]}` 在第 N/M 列后画加粗竖线（手册 70w8j2pj 表格工具参数），此前任意属性一律落到默认 centered 样式且不显示。

### 测试

- 差分语料 +3（three/tuack=N/tuack 多位置）至 63 例，63/63 与洛谷基准一致；49 项对抗自检 + 42+58 回归全绿。

## [1.2.12] - 2026-08-30

### 新增

- **快捷键（与洛谷手册完全对齐）**：标题升格/降格 `Mod+Shift+↑/↓`、加粗/斜体/删除线 `Mod+B/I/D`、行内公式 `Mod+M`、水平线 `Mod+Shift+H`、链接/图片 `Mod+Shift+L/I`、引用/行内代码/表格 `Mod+Shift+Q/1/2`、无序/有序/任务列表 `Mod+Shift+7/8/9`，均仅在 Markdown 编辑焦点形生生效；引用与列表键对选区逐行转换。

### 修复

- **GFM 裸 URL 尾置中文标点并入链接**（严格对齐洛谷）：`见 https://x.com/b。`、`（https://x.com）`、`见 www.a.com，去吧` 实测 remark-gfm 会把中文标点/汉字并入 URL 字面量；此前实现做了「中文友好地」剥离，与洛谷渲染不符。现已严格对齐。一并追加了 3 条差分语料 + 3 项对抗自检锁死。

### 定性说明

- 用户上报的「`$\mathcal{O}(n \log n)$ → $\ \log$」「`$\{$ → $LUOGUTOKENESC0END$」两类 breakage 来自旧 installation 里已被删除的 `media/luogu-linter.js`（仅存在于原首版 git 历史）；现行 v1.2.x 修复器对两类输入幂等不破坏，「test-bugsweep 一键排版幂等锁」已新增 4 项按 line 盾守护。

## [1.2.11] - 2026-08-30

### 文档

- README 大幅收敛排版（去掉装饰性 emoji，保留全部信息与 badge），结构对齐 v1.2.10 特性矩阵。

## [1.2.10] - 2026-08-29

### 修复（随 v1.2.9 对齐洛谷引入的 4 个 edge bug + 全量开源自检）

- **脚注引用不被还原**：占位符正则漏了 `FNREFTOKEN` 命名空间，正文 `[^把]` 渲染为原文遗留占位符。
- **脚注多行续行定义丢失正文**：惰性 `(.*?)` 在首行即空命中，导致「注一。\n    续行」被吞并正文变空；改贪婪 `(.*)`（dot 不跨行天然终止）。
- **单波浪 `~x~` 边界过严**：实测洛谷（remark-gfm）对 `a~b~c`、`中~x~文`、`约 ~5 元与~9~定` 全出 delete；旧实现只认空白/括号前缀，显著少于洛谷。改为 `(?<![~])~([^~\s](?:[^~\n]*[^~\s])?)~(?![~])`。
- **非字符串输入致渲染器崩溃**：`render(undefined)` 直接炸 `split`，现在归一为空串。

新增 45 项对抗性自检（状态泄漏/边界 URL/CJK/CRLF/ReDoS/XSS/幂等）+ 6 条差分语料锁死。全部测试：45+57+42+58 项通过。

## [1.2.9] - 2026-08-29

### 与洛谷渲染对齐（51 例差分语料分叉清零）

- **段内 `$$…$$` 按行内公式渲染**：洛谷现行编辑器（remark + remark-math）将独占块以外的 `$$` 视为行内公式；此前扩展会渲染为行间块，影响排版分栏。现在 `见 $$x+y$$ 公式` 与洛谷呈现一致（独占行块级 `$$` 语义不变）。
- **GFM 裸 URL 自动链接**：`https://example.com`、`www.example.com` 无需尖括号现在自动渲染为链接；尾置标点（句号/逗号/不配对右括号/中文标点）按 GFM 规则剥离，不误吞。
- **单波浪 `~删除线~`**：GFM 允许单字符删除线，此前仅支持 `~~双波浪~~`，现与洛谷一致支持单波浪（`` ~~~ 代码围栏不受影响 ``）。
- **GFM 脚注 `[^标记]`**：脚注定义（含缩进续行）在文末按首次引用次序编号集中渲染，正文引用处上标可互相跳转；多行定义保持源码行号不变。
- **表格对齐输出收敛**：分隔行无对齐标记时不再默认输出 `text-align:left`，与 remark 语义一致（`:---` 明确左对齐仍会输出）。

## [1.2.8] - 2026-08-29

### 修复

- **`$$` 行内/行间边界与洛谷对齐**：原实现用单条 `\$\$[\s\S]*?\$\$` 全文档配对，会把「文字 $$ s」跨空行错配成行间公式（而洛谷显示字面文本）。改为三级匹配：①开/闭 `$$` 独占行的块级公式（允许跨越空行，与洛谷一致）；②`$$x$$` 整行单行；③段内 `$$...$$` 绝不跨越空行。你的用例 `文字 $$ s` + 后续 `$$ …… s …… $$` 在预览中现与洛谷呈现完全一致。

## [1.2.7] - 2026-08-29

### 外观

- 更换侧边栏活动区图标：实心圆角徽标 + Markdown「M」与文档代码行，替代原有的空心方框薄 M 图标（跟随主题自动着色）。

## [1.2.6] - 2026-08-28

### 修复

- **CRLF（\r\n）文档全面兼容**：文件以 CRLF 换行时，每行末尾残留的 `\r` 会让一键修复的句末补句号、行尾空格规范化等所有"行尾判定"全部落空（表现为点修复提示已符合规范，实际问题面板仍有 Hint；整篇只有最后一行被补上句号），lint 的行尾空格检测同样失明。本版：lint 入口统一归一化换行；修复器逐行剥除 `\r` 处理后原样补回，文档换行风格保持不变。

## [1.2.5] - 2026-08-28

### 修复

- **句末句号规则改回逐行判定**：v1.2.4 引入的「段落感知」在洛谷题解常见的「一句一行、行间无空行」写法下，会把软换行的整段视为一个段落，导致一键修复只在段落最后一行补句号、前面各行的句号全部漏补。洛谷语境下每一行内容行都应是完整句子，恢复逐行判定（以 ，、：（ 等延续性标点结尾的行本来就会跳过，不会误伤真正跨行的句子）。lint 提示与一键修复同步生效。

## [1.2.4] - 2026-08-28

### 修复

- **「一键排版规范修复」按钮实际生效**：旧实现（fixSpacing）只会做盘古空格与空格合并，处理不了规范中的半角标点/公式/行尾等绝大多数条目，点击后肉眼无变化。替换为与 lint 同源的全量修复器 `autoFixLuoguStyle`：
  - §1.1 中文语境半角标点、括号 → 全角；句末自动补「。」（含公式结尾，保留硬换行两个空格）
  - §1.2 中文 ↔ 英文/数字/公式 两侧自动补半角空格
  - §1.3 全角标点两侧空格自动剥除（含「中文 ，中文」「$n \\le 10^5$ ，」两类形态）
  - §2 公式内部 `<= >= != ==` → `\le \ge \ne =`、`*` → `\times`、裸 `mod` → `\bmod`、全角标点 → 半角；公式碎拼 `$a$ + $b$` 自动合并；`$DFS$` 式英文误用 LaTeX 自动脱壳；正文 `2*3` → `$2 \times 3$`、纯文本 `O(…)` → `$\mathcal{O}(…)$`、`5e9` → `$5 \times 10^{9}$`
  - 连续空行压缩为一个、行尾空格规范化（恰好两个空格的硬换行保留）
  - 围栏代码、行内代码、数学块原文严格保留（占位符保护）
- **句末标点规则段落感知**：软换行跨行段落只在段落真正结束的末行判定/补句号，列表项逐项判定；彻底消除把半句话误补「。」的问题。修复成功提示改为「排版修复完成 ✓ / 剩余 N 条提示需人工处理」。

## [1.2.3] - 2026-08-28

### 排版规范检查对齐《洛谷主题库题解规范》与《专栏推荐规范》

- **§1.1 句末必须有标点**：普通中文段落（含公式结尾）缺少句末标点时提示补充「。」。
- **§1.2 中文 ↔ LaTeX 公式空格**：中文与行内公式紧贴（`设$n$为正整数`）提示加半角空格，与已有的中英文/中数字盘古之白合并为完整覆盖。
- **§1.3 中文标点严禁夹空格**：改为在原始行上判定（修复公式/链接被遮罩后产生的假空格误报），可正确捕获「$n \\le 10^5$ ，」类违规；行内代码与公式内部豁免。
- **§2 数学语言**：
  - 公式碎拼：`$a$ + $b$ = $c$` 提示合写进同一个 `$` 环境；
  - 公式内禁用 `<= >= != ==`（建议 `\le`/`\ge`/`\ne`/`=`）、禁用 `*` 作乘号（`\times`/`\cdot`）、裸 `mod` → `\bmod`；
  - 公式内混入中文或全角标点提示；
  - 非数学内容（英文单词/算法名/题目名，如 `$DFS$`）误用 LaTeX 环境提示；
  - 正文中数字 `*` 数字的乘号、纯文本 `O(…)` 复杂度、`5e9` 式大数字分别提示 `\times`、`\mathcal{O}`、科学计数法。
- **§3 代码块语言**：无语言声明的 ``` 围栏提示显式指定编程语言。
- 修复：图片/链接 own-line 结尾 `]` 被遮罩导致句末判定误报；「公式内混入中文」正则交替误写为整串匹配导致永不触发的缺陷。
- 规则测试扩充至 56 项，规范示例文档零误报。

## [1.2.2] - 2026-08-28

### 修复

- **任意深度平衡括号链接/图片**：`[[[]]](sss)`、`[a[b[c]]d](u)` 等多层嵌套链接文本现在与洛谷（CommonMark）行为一致地正确渲染；链接文本内嵌套图片 `[![alt](img)](url)` 也已支持。实现上将行内正则替换升级为逃逸安全的平衡括号深度扫描（图片与链接共用），保持未闭合/非链接情形下的字面文本回退。
- 顺带修复链接目标写成 `<https://...>` 尖括号形式时会被自动链接规则抢先吞掉的边界问题（扫描先于自动链接执行）。

### 新增

- **洛谷排版规范检查 → VSCode 问题面板（Hint 级）**：新增 `style-lint.js`，按洛谷社区通行排版规范给出轻量提示（与语法 Error/Warning 区分，source 标注「luogu-排版」，精确到列）：
  - 中英文、中文与数字之间建议空格（盘古之白）
  - 中文语境下的半角标点建议全角（含推荐字符提示）
  - 中文语境下的括号建议全角「（）」
  - 全角标点两侧不需要空格
  - 行尾多余空格（洛谷硬换行需恰好两个空格）
  - 连续多余空行、标题层级跳跃
  - 围栏代码块、行内代码、数学公式、裸 URL 与链接目标区域自动豁免


