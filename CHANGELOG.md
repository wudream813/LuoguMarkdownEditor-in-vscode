# Changelog
## [1.2.4] - 2026-08-28

### 修复

- **「一键排版规范修复」按钮实际生效**：旧实现（fixSpacing）只会做盘古空格与空格合并，处理不了规范中的半角标点/公式/行尾等绝大多数条目，点击后肉眼无变化。替换为与 lint 同源的全量修复器 `autoFixLuoguStyle`：
  - §1.1 中文语境半角标点、括号 → 全角；句末自动补「。」（含公式结尾，保留硬换行两个空格）
  - §1.2 中文 ↔ 英文/数字/公式 两侧自动补半角空格
  - §1.3 全角标点两侧空格自动剥除（含「中文 ，中文」「$n \\le 10^5$ ，」两类形态）
  - §2 公式内部 `<= >= != ==` → `\le \ge \ne =`、`*` → `\times`、裸 `mod` → `\bmod`、全角标点 → 半角；公式碎拼 `$a$ + $b$` 自动合并；`$DFS$` 式英文误用 LaTeX 自动脱壳；正文 `2*3` → `$2 \times 3$`、纯文本 `O(…)` → `$\mathcal{O}(…)$`、`5e9` → `$5 \times 10^{9}$`
  - 连续空行压缩为一个、行尾空格规范化（恰好两个空格的硬换行保留）
  - 围栏代码、行内代码、数学块原文严格保留（占位符保护）
- **句末标点规则段落感知**：软换行跨行段落只在段落真正结束的末行判定/补句号，列表项逐项判定；彻底消除把半句话误补「。」的问题。修复成功提示改为「排版修复完成 ✓ / 剩余 N 条提示需人工处理」。

## [1.2.3] - 2026-08-28

### 排版规范检查对齐《洛谷主题库题解规范》与《专栏推荐规范》

- **§1.1 句末必须有标点**：普通中文段落（含公式结尾）缺少句末标点时提示补充「。」。
- **§1.2 中文 ↔ LaTeX 公式空格**：中文与行内公式紧贴（`设$n$为正整数`）提示加半角空格，与已有的中英文/中数字盘古之白合并为完整覆盖。
- **§1.3 中文标点严禁夹空格**：改为在原始行上判定（修复公式/链接被遮罩后产生的假空格误报），可正确捕获「$n \\le 10^5$ ，」类违规；行内代码与公式内部豁免。
- **§2 数学语言**：
  - 公式碎拼：`$a$ + $b$ = $c$` 提示合写进同一个 `$` 环境；
  - 公式内禁用 `<= >= != ==`（建议 `\le`/`\ge`/`\ne`/`=`）、禁用 `*` 作乘号（`\times`/`\cdot`）、裸 `mod` → `\bmod`；
  - 公式内混入中文或全角标点提示；
  - 非数学内容（英文单词/算法名/题目名，如 `$DFS$`）误用 LaTeX 环境提示；
  - 正文中数字 `*` 数字的乘号、纯文本 `O(…)` 复杂度、`5e9` 式大数字分别提示 `\times`、`\mathcal{O}`、科学计数法。
- **§3 代码块语言**：无语言声明的 ``` 围栏提示显式指定编程语言。
- 修复：图片/链接 own-line 结尾 `]` 被遮罩导致句末判定误报；「公式内混入中文」正则交替误写为整串匹配导致永不触发的缺陷。
- 规则测试扩充至 56 项，规范示例文档零误报。

## [1.2.2] - 2026-08-28

### 修复

- **任意深度平衡括号链接/图片**：`[[[]]](sss)`、`[a[b[c]]d](u)` 等多层嵌套链接文本现在与洛谷（CommonMark）行为一致地正确渲染；链接文本内嵌套图片 `[![alt](img)](url)` 也已支持。实现上将行内正则替换升级为逃逸安全的平衡括号深度扫描（图片与链接共用），保持未闭合/非链接情形下的字面文本回退。
- 顺带修复链接目标写成 `<https://...>` 尖括号形式时会被自动链接规则抢先吞掉的边界问题（扫描先于自动链接执行）。

### 新增

- **洛谷排版规范检查 → VSCode 问题面板（Hint 级）**：新增 `style-lint.js`，按洛谷社区通行排版规范给出轻量提示（与语法 Error/Warning 区分，source 标注「luogu-排版」，精确到列）：
  - 中英文、中文与数字之间建议空格（盘古之白）
  - 中文语境下的半角标点建议全角（含推荐字符提示）
  - 中文语境下的括号建议全角「（）」
  - 全角标点两侧不需要空格
  - 行尾多余空格（洛谷硬换行需恰好两个空格）
  - 连续多余空行、标题层级跳跃
  - 围栏代码块、行内代码、数学公式、裸 URL 与链接目标区域自动豁免


